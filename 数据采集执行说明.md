# Qwen3.6-35B-A3B-w8a8 数据采集执行说明

本文用于在昇腾 310P3 单机多卡、TP=2、vllm-ascend 环境下，采集 Qwen3.6-35B-A3B-w8a8 的 Prefill 和 Decode 性能数据，并判断 CANN 实际使用的 GEMM tiling 参数。

## 1. 采集目标与关键结论

本次采集需要区分三类数据：

| 数据 | 含义 | 采集方式 |
|---|---|---|
| `M、N` | GEMM 输出矩阵 `C[M,N]` 的逻辑形状 | Profiler 的算子输入/输出 Shape |
| `baseM、baseN` | Cube 内部的基础分块 | CANN 算子属性或 TilingData |
| `singleCoreM、singleCoreN` | 一个 AIC 实际负责的输出子矩阵 | CANN `TCubeTiling`/TilingData |

对本文的 Overlap 公式，通常将一个 AIC 负责的输出子矩阵作为波次 Tile，因此优先使用：

```text
Tm = singleCoreM
Tn = singleCoreN
P  = usedCoreNum
```

如果待分析的融合算子定义的是 Cube 内部基础块，则应使用 `baseM/baseN`，不能把 `baseM/baseN` 和 `singleCoreM/singleCoreN` 混为一谈。CANN 的 `TCubeTiling` 同时包含这些字段，实际值由运行时 `GetTiling` 根据 Shape、数据类型、SoC 和 Kernel 策略生成。

仅修改 `--max-model-len` 或增加 `torch_profiler_record_shapes`，不能得到真实 `Tm/Tn`。前者只是上下文长度上限，后者最多记录算子 Shape，不是 Cube tiling 参数。

## 2. TP=2 时需要固定的输入形状

### 2.1 2048 token 必须由请求决定

`--max-model-len 20480` 并不表示每次请求输入 20480 token。实际 Prefill 长度来自客户端请求，必须发送恰好 2048 个 token id，并检查返回值：

```json
"usage": {
  "prompt_tokens": 2048
}
```

不要只发送一段“看起来很长”的文本，因为 tokenizer 后的 token 数量不一定是 2048。

### 2.2 `C[M,N]` 中的 M

- Prefill：单请求、2048 token 时，通常 `M=2048`。
- Decode：单序列每次生成一个 token 时，通常 `M=1`。
- 如果同时处理 `B` 个 Decode 序列，则 Decode GEMM 的 `M` 可能为 `B`。

`M` 是 GEMM 的逻辑行数，不是 Tile 宽度，因此不会因为硬件 Tile 是 44、64 或其他数值而变成 44 或 64。

### 2.3 `C[M,N]` 中的 N

TP=2 时，线性层的输出维度通常按 Tensor Parallel 切分，每个 Rank 看到的是本地输出宽度，因此 `N` 应从实际 GEMM 算子 Shape 读取。不同算子（QKV、Attention 输出投影、MLP 上投影/下投影、LM Head）可能有不同的 `N`，不能用一个固定值代替全部算子。

## 3. 推荐的基准启动命令

下面的命令用于得到稳定的 TP=2、单请求、2048-token Prefill 基线。`--max-model-len 4096` 仅适用于本次只测试 2048 token 输入和少量输出的情况；如果后续需要更长上下文，可以保留原来的 20480。

```bash
vllm serve /home/models/Qwen3.6-35B-A3B-w8a8 \
  --host 127.0.0.1 \
  --port 3333 \
  --tensor-parallel-size 2 \
  --served-model-name qwen3.6 \
  --trust-remote-code \
  --dtype float16 \
  --max-num-seqs 1 \
  --max-num-batched-tokens 2048 \
  --max-model-len 4096 \
  --gpu-memory-utilization 0.90 \
  --additional-config \
  '{"ascend_compilation_config":{"fuse_norm_quant":false,"enable_npugraph_ex":false}}' \
  --compilation-config \
  '{"cudagraph_mode":"FULL_DECODE_ONLY","cudagraph_capture_sizes":[1]}' \
  --mamba-ssm-cache-dtype float16 \
  --mamba-cache-mode align \
  --enable-prompt-tokens-details \
  --profiler-config \
  '{"profiler":"torch","torch_profiler_dir":"/home/xxx/vllm_ascend-developer/profiling/Qwen36_TP2_2048","torch_profiler_with_stack":false}'
```

说明：

1. `--tensor-parallel-size 2` 与 `-tp 2` 等价，建议使用完整参数名。
2. `--max-num-seqs 1` 防止多个请求合并，便于确认 `M=2048` 或 Decode 时 `M=1`。
3. `--max-num-batched-tokens 2048` 配合单请求，使 2048-token Prefill 尽量一次完成。
4. 删除 `--enable-prefix-caching`，避免命中前缀缓存后跳过 Prefill。
5. 删除 `--enable-chunked-prefill`，避免 2048 token 被拆成多个 Prefill chunk。
6. 删除 `--speculative_config`，先采集不带 MTP 的基准 Decode；MTP 应单独作为另一组实验。
7. 两个 `--additional-config` 必须合并，避免后一个参数覆盖前一个参数。
8. 模型如果需要显式的量化参数，应沿用当前已经验证可运行的量化配置；切换量化参数不是获取 `Tm/Tn` 的方法。

## 4. 生成恰好 2048 token 的请求

可以通过 tokenizer 生成固定长度的 token id，再调用 `/v1/completions`。示例：

```python
import requests
from transformers import AutoTokenizer

MODEL_PATH = "/home/models/Qwen3.6-35B-A3B-w8a8"
URL = "http://127.0.0.1:3333/v1/completions"

tokenizer = AutoTokenizer.from_pretrained(
    MODEL_PATH,
    trust_remote_code=True,
)

unit = tokenizer.encode("hello ", add_special_tokens=False)
assert unit, "tokenizer 没有产生 token"
prompt_ids = (unit * ((2048 + len(unit) - 1) // len(unit)))[:2048]
assert len(prompt_ids) == 2048

payload = {
    "model": "qwen3.6",
    "prompt": prompt_ids,
    "max_tokens": 1,
    "temperature": 0,
}
response = requests.post(URL, json=payload, timeout=600)
response.raise_for_status()
result = response.json()
print(result.get("usage"))
assert result["usage"]["prompt_tokens"] == 2048
```

## 5. Profiler 执行顺序

编译和 warmup 不要放在正式采集窗口内：

```bash
# 1. 服务启动后，先发送一次 2048-token 请求完成 warmup/编译

# 2. 开始采集
curl -X POST http://127.0.0.1:3333/start_profile

# 3. 再发送一次上面的 2048-token 请求
python send_2048.py

# 4. 停止采集
curl -X POST http://127.0.0.1:3333/stop_profile
```

vllm-ascend 的在线 profiler 通过 `/start_profile` 和 `/stop_profile` 控制采集，输出目录通常包含 `*_ascend_pt`、`operator_details.csv`、`kernel_details.csv` 等文件。建议关闭 Python stack，即 `torch_profiler_with_stack=false`，减少采集开销。

## 6. 获取真实 `Tm/Tn` 的必要条件

当前 vllm-ascend 源码中的 [torch_npu_profiler.py](/home/ubuntu/codes/npu_qwen36/vllm-ascend/vllm_ascend/profiler/torch_npu_profiler.py:49) 使用了：

```python
op_attr=False
```

因此，当前版本没有一个可以直接通过 `--profiler-config` 设置的 `op_attr` 启动参数。要尝试导出 ACLNN 算子属性，需要在采集用分支中改为：

```python
op_attr=True
```

然后重新启动服务并重新采集。`op_attr` 主要对 ACLNN 算子生效，不能保证 vLLM 自定义 Kernel 一定导出 TilingData。若性能文件中没有以下字段：

```text
baseM, baseN, singleCoreM, singleCoreN, usedCoreNum, tiling
```

则说明当前算子没有通过 PyTorch profiler 导出真实 tiling，不能根据 `operator_details.csv` 中的 Shape 反推真实 `Tm/Tn`。此时需要使用 CANN `msprof/MindStudio` 的算子属性采集，或在自定义算子的 host tiling 函数中打印 `TCubeTiling`。

`Split-K=2` 也不能通过 `vllm serve` 的普通启动参数强制打开。它由实际 GEMM Kernel 和 CANN tiling 策略决定；只有在 Kernel 属性或 TilingData 中确认 split-K 为 2，才能将该次采集归入 “Decode Split-K=2”。

## 7. 三个场景的采集方法

### 7.1 Prefill Attention

1. 使用上面的启动命令。
2. 发送恰好 2048 token 的请求。
3. 在 profiler 中筛选 Prefill 阶段的 Attention/QKV/投影 GEMM。
4. 重点检查 `M=2048` 的 GEMM，并记录其 `N`、`singleCoreM/N`、`baseM/N`、`usedCoreNum` 和耗时。

### 7.2 Decode 默认 Tile

1. 先在 profiler 外建立 2048-token 上下文。
2. 开始 profiler。
3. 生成一个或多个 token，不使用 MTP。
4. 筛选 Decode GEMM，单序列时通常 `M=1`。
5. 记录 CANN 实际选择的 `singleCoreM/N` 或 `baseM/N`，不要把预估 Tile 大小当作真实值。

### 7.3 Decode Split-K=2

1. 使用与 Decode 默认 Tile 相同的输入和 TP=2 配置。
2. 必须从 Kernel/TilingData 中确认 split-K 确实为 2。
3. 如果运行时没有选择 split-K=2，不能仅凭启动参数或理论公式把该数据标记为 Split-K=2；需要对应的 Kernel 配置、源码改动或 CANN tiling 控制。

## 8. Overlap 空间计算

采集到真实参数后，对每组候选融合算子对使用：

```text
Tile 数量 = ceil(M / Tm) * ceil(N / Tn)
W         = ceil(Tile 数量 / P)
可重叠比例 = (W - 1) / W
T_hide    = min(T_matmul, T_AllReduce) * (W - 1) / W
Overlap   = T_hide / T_base
```

其中：

- `M、N`：当前 GEMM 输出矩阵逻辑 Shape。
- `Tm、Tn`：优先取 `singleCoreM、singleCoreN`，但必须根据融合 Kernel 的通信粒度确认。
- `P`：优先取 `usedCoreNum`，不要默认填物理卡的 AIC 总数。
- `T_matmul`、`T_AllReduce`：从同一采集窗口的算子耗时读取。
- `T_base`：未融合基线中对应算子链的总耗时。

普通 vLLM 路径通常是独立 GEMM 后再执行 AllReduce，因此按上述公式计算的是“理论可利用的 Overlap 空间”，不等同于当前代码已经实现了通算融合。

## 9. 常见误区检查表

| 误区 | 正确做法 |
|---|---|
| 认为 `max-model-len=20480` 就是输入 2048 | 通过请求发送 2048 个 token id，并检查 `usage.prompt_tokens` |
| 把 `M` 当成硬件 Tile 大小 | Prefill 单请求时 `M=2048`，Decode 单序列时通常 `M=1` |
| 用 `torch_profiler_record_shapes` 获取 `Tm/Tn` | 它只能帮助确认逻辑 Shape；真实 tiling 需要 `op_attr`/TilingData |
| 直接把 `baseM/baseN` 当作波次 Tile | 先确认融合通信粒度；通常波次输出 Tile 使用 `singleCoreM/N` |
| 在启用 Prefix Cache 时采集 Prefill | 关闭 Prefix Cache，避免 Prefill 被缓存跳过 |
| 用 MTP 数据代表普通 Decode | MTP 单独采集并单独标记 |
| 默认认为 Decode 使用 Split-K=2 | 必须从 Kernel/TilingData 中确认，启动参数不能保证这一点 |

## 10. 参考资料

- [vllm-ascend Service Profiling Guide](https://docs.vllm.ai/projects/ascend/en/main/developer_guide/performance_and_debug/service_profiling_guide.html)
- [torch_npu `_ExperimentalConfig` 的 `op_attr` 说明](https://gitcode.com/Ascend/op-plugin/blob/26.0.0/docs/zh/custom_APIs/torch_npu-profiler/torch_npu-profiler-_ExperimentalConfig.md)
- [CANN `TCubeTiling` API](https://www.hiascend.com/document/detail/en/canncommercial/850/API/ascendcopapi/atlas_ascendc_api_07_0673.html)

